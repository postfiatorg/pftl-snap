import styled from "styled-components";
import { Typography } from "../Typography";
export var FormControlHintRoot = styled(Typography).attrs({
  variant: "caption"
})(function () {
  return {
    marginLeft: "5px"
  };
});