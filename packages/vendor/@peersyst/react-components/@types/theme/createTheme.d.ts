import { CreateTheme, Theme } from "@peersyst/react-components-core";
export declare function createTheme({ loader, icons, typography, fonts, palette, breakpoints, borderRadius, zIndex, ...rest }: CreateTheme): Theme;
